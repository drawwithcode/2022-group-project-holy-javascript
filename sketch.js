function setup() {
  createCanvas(windowWidth, windowHeight);
  // put setup code here
}

function draw() {
  // put drawing code here
  fill(0);
  circle(20, 30, 100);
  text("peddi porco dio", width / 2, height / 2);
}
